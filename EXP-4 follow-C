#include <stdio.h>
#include <stdbool.h>

struct Production {
    char nonTerminal;
    char rule[10]; 
};

void computeFollowSet(char nonTerminal, struct Production productions[], int numProductions) {
}

int main() {
    struct Production productions[] = {
        {'S', "AB"},
        {'A', "aA"},
        {'A', ""},
        {'B', "bB"},
        {'B', ""},
    };

    int numProductions = sizeof(productions) / sizeof(productions[0]);

    char nonTerminalToCompute = 'S';
    computeFollowSet(nonTerminalToCompute, productions, numProductions);

    return 0;
}
int main() {
